import * as CP from './copyprops';
export const copyprops = CP.copyprops;

import * as DM from './deepMerge';
export const deepMerge = DM.deepMerge;

import * as DU from './deepUpdate';
export const deepUpdate = DU.deepUpdate;

import * as D from './distinct';
export const distinct = D.distinct;

import * as GB from './groupBy';
export const groupBy = GB.groupBy;

import * as PU from './promiseUtils';
export const resolveAfter = PU.resolveAfter;
export const ensureDelay = PU.ensureDelay;
export const retryPromise = PU.retryPromise;

import * as T from './throttle';
export const throttle = T.throttle;
